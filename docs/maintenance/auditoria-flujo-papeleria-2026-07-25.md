# Auditoría del flujo membrete/receta/orden/impresión/PDF/Word — 2026-07-25

**27 confirmados** {'P0': 0, 'P1': 20, 'P2': 6, 'P3': 1}.

---

### 1. [P1] src/lib/receta-word.ts:69 — Word de receta y de orden (vs. Pantalla, Imprimir y PDF, que sí usan el diseño)
**Word de RECETA/ORDEN ignora el diseño completo del médico (disenoCompletoDataUrl): pantalla/Imprimir/PDF muestran su arte, Word sale con plantilla de texto**
- Impacto: Es la incongruencia central que el dueño exige cerrar. RecetaDocumento decide con dos campos DISTINTOS: HojaCustom se dibuja cuando existe `recetaConfig.disenoCompletoDataUrl` (arte de página completa con campos calibrados) y es lo que renderizan pantalla, Imprimir y PDF. Pero receta-word.ts SOLO conoce `recetaConfig.membreteDataUrl` (el membrete pequeño de la plantilla generada); nunca lee `disenoCompletoDataUrl`. Resultado: el médico que subió su receta diseñada obtiene un Word con encabezado de TEXTO auto-generado (líneas 78-87) o, si además tiene membreteDataUrl, una imagen chica de 140px arriba de una maqueta que no es su diseño. Dos artefactos del mismo acto médico que no se parecen.
- Repro: Configuración → Recetas: subir un diseño de receta de página completa (queda en disenoCompletoDataUrl). Generar una receta → la Pantalla, Imprimir y PDF muestran el diseño con los campos calibrados; pulsar 'Word' → el .doc sale con el encabezado de texto (nombre/cédula) y lista limpia, SIN el diseño.
- Arreglo: En construirRecetaHTML, priorizar `recetaConfig.disenoCompletoDataUrl` sobre `membreteDataUrl` como imagen de fondo a página completa (mismo criterio que HojaCustom: object-fit contain, tamaño de hoja = disenoWidthMm/HeightMm orientados), y superponer los datos del paciente en las coordenadas de `disenoCampos` con posición absoluta en pt/%, igual que RecetaDocumento. Cuando no hay diseño ni membrete, mantener la plantilla de texto (default congruente con HojaGenerada). Así las 4 salidas comparten el mismo arte.

### 2. [P1] src/lib/nota-word.ts:37 — Word de nota (vs. Pantalla, Imprimir y PDF de la nota, que sí usan el membrete)
**Word de NOTA nunca usa la hoja membretada: pantalla/Imprimir/PDF la pintan, el .doc sale con encabezado de texto**
- Impacto: Mismo patrón que la receta pero en la nota. El encabezado de nota-word es SIEMPRE texto (líneas 37-42) y jamás lee `config.notaMembreteDataUrl` ni la resolución por-médico. La pantalla/Imprimir/PDF de la nota sí resuelven y pintan el membrete (`medMembrete`/`notaMembreteDataUrl`, nota/page.tsx líneas 174-190). Además, el llamador `descargarNotaWord(nota, config, extra)` ni siquiera recibe el membrete ya resuelto por médico que calculó la página, así que aunque nota-word quisiera pintarlo, no lo tiene.
- Repro: Configuración → Cuenta: subir hoja membretada de nota. Abrir una nota → Imprimir/PDF muestran el membrete de fondo; pulsar 'Word' → el .doc sale con encabezado de texto (nombre/especialidad/cédula), incongruente con las otras tres salidas.
- Arreglo: Pasar a descargarNotaWord el membrete YA resuelto (medMembrete?.url ?? config.notaMembreteDataUrl) y sus márgenes; en construirNotaHTML, si hay membrete, usarlo como imagen de fondo/encabezado a sangre con el mismo padding (mMemb) en vez del encabezado de texto. Sin membrete, conservar el encabezado de texto default.

### 3. [P1] src/app/(dashboard)/nota/[patientId]/[notaId]/page.tsx:274 — PDF de nota con hoja membretada y 2+ páginas (vs. Imprimir, que repite el membrete y reserva la banda en TODAS las hojas)
**NOTA multipágina en PDF: el membrete solo aparece en la hoja 1 y el texto se empalma con la banda del membrete en hojas 2+ (Imprimir sí lo repite)**
- Impacto: El membrete de fondo se pinta con `<img className='membrete-bg' position:absolute; top:0; height:auto>` (una sola vez, arriba del #doc) y el margen superior del membrete es padding del #doc (solo aplica a la hoja 1). descargarComoPDF rasteriza #doc con html2canvas y lo rebana en hojas carta: la hoja 1 sale con membrete, pero las hojas 2+ salen SIN membrete y con el texto pegado al borde superior, invadiendo la zona donde va impreso el membrete físico. En cambio Imprimir usa formato:'membrete' con tabla thead/tfoot + membrete position:fixed (print-element.ts 88-125) que repite el membrete y reserva las bandas superior/inferior en cada página. Es exactamente la queja del dueño ('sale sin membrete / se empalma'), pero solo en el PDF y a partir de la 2ª hoja.
- Repro: Con hoja membretada configurada, abrir una nota larga (que ocupe 2+ hojas) y pulsar 'Descargar PDF'. Hoja 1 con membrete; hoja 2 sin membrete y con el texto encimado en la banda superior. Imprimir la misma nota sale correcto en ambas hojas.
- Arreglo: Hacer que el PDF pagine igual que Imprimir: envolver el contenido en el mismo esquema thead/tfoot espaciador con membrete repetido por página, o generar el PDF de la nota membretada vía el mismo motor de print-element (popup → 'Guardar como PDF') en lugar de html2canvas. Alternativa mínima: paginar el #doc en hojas discretas .nota-sheet (como RecetaDocumento) con el membrete de fondo por hoja y page-break inline, para que pantalla y PDF coincidan con Imprimir.

### 4. [P1] src/app/(dashboard)/referencia/[patientId]/page.tsx:191 — Referencia: Pantalla, Imprimir y PDF (las tres con encabezado de texto; la nota membretada de la clínica se ignora por completo)
**La CARTA DE REFERENCIA nunca usa la hoja membretada en ninguna salida, aunque la nota de la misma clínica sí la usa**
- Impacto: La referencia es una carta formal —el documento MÁS natural para el papel membretado— y sin embargo su encabezado es siempre texto (líneas 191-197): jamás lee `config.notaMembreteDataUrl` ni `notaMembretePorMedico`. Viola la ley del dueño ('si hay membrete, úsalo') y es incongruente con la NOTA, que sí pinta el membrete. El médico con papel membretado obtiene notas membretadas pero referencias con encabezado de texto genérico.
- Repro: Con hoja membretada de nota configurada: abrir una nota → sale membretada; generar una Carta de referencia del mismo paciente → sale con encabezado de texto, sin el membrete, en Pantalla, Imprimir y PDF.
- Arreglo: Reusar en la referencia la misma resolución de membrete de la nota (medMembrete/notaMembreteDataUrl + márgenes) y, cuando exista, pintarlo como fondo con padding mMemb e imprimir con formato:'membrete' (hoy usa formato:'carta', línea 127). Sin membrete, conservar el encabezado de texto default. Así referencia queda congruente con la nota y entre sus tres salidas.

### 5. [P1] src/lib/nota-word.ts:72 — Word de nota (contradice al Word de receta/orden y a la Pantalla de la nota)
**nota-word declara 'Negadas / no referidas' ante alergias vacías: reintroduce la afirmación clínica inventada que ya se reparó en receta-word**
- Impacto: Es el MISMO patrón ya corregido en receta-word.ts (líneas 102-118: campo vacío ≠ 'Negadas', se usa 'Sin registro en el expediente') pero vivo en otra salida. nota-word imprime `extra?.alergias || 'Negadas / no referidas'`, afirmando que se interrogó y el paciente negó cuando en realidad el campo está vacío. La Pantalla/Imprimir/PDF de la nota distinguen TRES estados (nota/page.tsx 317-319: paciente no leído → 'NO DISPONIBLE'); su propio Word colapsa a dos y puede inventar la negación. Riesgo clínico-legal (NOM-004) y contradicción entre salidas del mismo documento.
- Repro: Paciente sin alergias registradas: abrir su nota → pantalla dice según el caso; descargar 'Word' → el .doc afirma 'ALERGIAS: Negadas / no referidas' aunque nunca se interrogó.
- Arreglo: Alinear nota-word con receta-word: distinguir vacío ('Sin registro en el expediente') de negación afirmada, y propagar el 3er estado (paciente no disponible) que ya maneja la pantalla. Unificar el texto default entre receta-word, nota-word y las pantallas.

### 6. [P1] src/app/(dashboard)/receta/[patientId]/[notaId]/page.tsx:331 — PDF de receta y orden con diseño APAISADO que no cabe en carta (vs. Imprimir, que orienta con @page)
**PDF de receta/orden fuerza orientation:'portrait' aunque la hoja efectiva sea apaisada; Imprimir usa la orientación real (@page cssPage)**
- Impacto: descargarPDF pasa siempre `orientation: 'portrait'` con `format:[host.widthMm, host.heightMm]`. Para el caso apaisado común (media carta horizontal con imprimirEn='carta') esHostCarta=true → host=carta vertical y coincide, así que no se nota. Pero cuando el diseño apaisado NO cabe en carta (esHostCarta=false, p. ej. arte carta-horizontal 279×216), dimensionesImpresion devuelve host apaisado y el @page de Imprimir usa cssPage='279mm 216mm' (horizontal), mientras el PDF sigue en portrait → jsPDF gira/recorta el arte. Incongruencia de orientación entre PDF e Imprimir para ese estado. (orden/page.tsx línea 463 tiene el mismo hardcode.)
- Repro: Subir un diseño de receta apaisado más ancho que carta; generar receta → Imprimir sale horizontal correcto; 'Descargar PDF' sale en vertical con el diseño girado/recortado.
- Arreglo: Derivar la orientación del host real: `orientation: host.widthMm > host.heightMm ? 'landscape' : 'portrait'` en receta y orden, para que el PDF respete la misma hoja orientada que @page/Imprimir.

### 7. [P1] src/lib/receta-word.ts:69 — WORD de receta y orden (incongruente con PANTALLA/IMPRIMIR/PDF)
**El Word de receta/orden IGNORA el diseño propio (disenoCompletoDataUrl): imprime plantilla limpia mientras Imprimir/PDF usan el arte del médico**
- Impacto: La ley del dueño es 'si hay receta diseñada, ÚSALA en las CUATRO salidas, incluso Word'. Pero construirRecetaHTML solo lee recetaConfig.membreteDataUrl (el membrete-encabezado) y NUNCA lee recetaConfig.disenoCompletoDataUrl, disenoCampos, disenoMargenes, disenoWidthMm/HeightMm. Cuando el médico subió su recetario COMPLETO (el caso que el dueño insiste), Imprimir y PDF renderizan HojaCustom con esa imagen a página completa y los campos calibrados encima; el Word en cambio genera un encabezado de texto + cuerpo 'limpio'. Dos papeles del mismo acto médico con aspecto totalmente distinto. El botón Word se muestra siempre (receta page:478), sin importar usaDisenoPropio (page:292), así que el médico con arte propio SIEMPRE obtiene un Word que no es su recetario.
- Repro: Config con disenoCompletoDataUrl subido (recetario completo) + disenoCampos calibrados. En /receta/... pulsar Imprimir o Descargar PDF => sale el arte del médico con datos encima. Pulsar Word => .doc con encabezado de texto genérico y lista de medicamentos, SIN el arte. Incongruente.
- Arreglo: En construirRecetaHTML detectar disenoCompletoDataUrl (absolutizar igual que membreteSrc, líneas 69-75). Si existe, emitir la hoja como fondo a página completa usando el truco robusto de Word HTML que SÍ imprime en Word/Mac: una tabla de una sola celda del tamaño del papel con la imagen como fondo de celda (<td background="IMG" width=... height=...>) o <v:rect> VML con fill de imagen, y dentro posicionar los campos (paciente/folio/firma/QR) con position:absolute en % según disenoCampos — replicando HojaCustom. Si solo hay membreteDataUrl, mantener el encabezado imagen actual. Sin diseño => plantilla generada actual. Así las 4 salidas coinciden.

### 8. [P1] src/lib/nota-word.ts:37 — WORD de nota (incongruente con IMPRIMIR/PDF que usan la hoja membretada)
**El Word de NOTA nunca embebe la hoja membretada: siempre emite encabezado de texto aunque exista membrete que SÍ sale en Imprimir/PDF**
- Impacto: construirNotaHTML arma SIEMPRE el encabezado de texto (nombre/cédula/establecimiento, líneas 37-42) y nunca considera un membrete. Peor aún, descargarNotaWord (línea 93) ni siquiera recibe la URL del membrete. La pantalla de nota SÍ resuelve el membrete por médico (nota page:175 entradaPorMedico(notaMembretePorMedico)) y lo pinta como fondo en Imprimir y PDF. Resultado: el Dr subió su hoja membretada, Imprimir/PDF la muestran, y el Word sale con un encabezado de texto distinto — exactamente la queja 'no me pones el membrete' pero trasladada a la salida Word.
- Repro: Config con notaMembretePorMedico (o membrete) válido. En /nota/... Imprimir o PDF => membrete de fondo. Descargar Word => encabezado de texto, sin membrete.
- Arreglo: Pasar a descargarNotaWord el membrete ya resuelto (mismo valor que usa la pantalla, nota page:190) y en construirNotaHTML, si hay membrete, sustituir el encabezado de texto por la imagen (idealmente como fondo de celda de tabla a página completa, y para notas multipágina repetirla vía thead como hace print-element para que el pie no caiga a media hoja). Sin membrete => encabezado de texto actual.

### 9. [P1] src/lib/nota-word.ts:72 — WORD de nota (contradice PANTALLA/IMPRIMIR/PDF de la misma nota)
**El Word de nota afirma 'Negadas / no referidas' cuando el paciente no se pudo leer — mismo bug de dos-estados que ya se corrigió en receta-word**
- Impacto: nota-word línea 72 hace `extra?.alergias || 'Negadas / no referidas'` — colapsa a dos estados. La receta-word YA arregló este patrón (líneas 102-118: distingue vacío de negadas). Cuando el paciente no se pudo cargar, la pantalla/print de la nota muestra 'NO DISPONIBLE — verificar con el paciente' (nota page:317-319) pero el Word estampa 'Negadas / no referidas' en un documento legal NOM-004. Es un dato clínico inventado y contradice a las otras 3 salidas. Además, cuando extra.alergias viene vacío pero el paciente sí existe, también afirma 'Negadas' sin distinguir 'no interrogado'.
- Repro: Abrir nota cuyo paciente falla al cargar (o sin alergias registradas), pulsar Word => 'ALERGIAS: Negadas / no referidas'. Comparar con la pantalla/PDF de la misma nota que dice 'NO DISPONIBLE — verificar con el paciente'.
- Arreglo: Reflejar los tres estados de nota page:317-319 y el criterio de receta-word: pasar a NotaWordExtra una señal de 'paciente no disponible' y usar 'Sin registro en el expediente' / 'NO DISPONIBLE — verificar con el paciente' en vez de afirmar 'Negadas' desde un campo vacío.

### 10. [P1] src/app/(dashboard)/nota/[patientId]/[notaId]/page.tsx:68 — Descargar PDF de la NOTA vs Imprimir (estado: CON hoja membretada, 2+ páginas)
**Nota multipágina con membrete: el PDF (html2canvas) no repite el membrete ni reserva las bandas por página; Imprimir sí**
- Impacto: Incompatibilidad directa con la ley del dueño ('el mismo documento debe verse igual en las 4 salidas'). Imprimir usa el motor de print-element en formato 'membrete' (print-element.ts:88-126): envuelve el contenido en una tabla con thead/tfoot espaciadores que el navegador REPITE por hoja + convierte .membrete-bg a position:fixed → el membrete y sus márgenes (mMemb) salen en TODAS las páginas. El PDF, en cambio, hace descargarComoPDF(#doc) → html2canvas rasteriza el #doc tal cual: el <img className='membrete-bg'> está en position:absolute; top:0; height:auto (page.tsx:274-275), así que aparece UNA sola vez arriba de la hoja 1; el padding superior/inferior (mMemb) también existe una sola vez para todo el bloque. En la hoja 2+ del PDF no hay membrete y el texto arranca pegado al borde superior y se derrama sobre la zona del pie membretado. Es el mismo patrón del bug ya reparado en Imprimir, pero vivo en la salida PDF.
- Repro: Configurar hoja membretada de nota (Config → Cuenta). Abrir una nota firmada larga (2+ páginas impresas). Pulsar Imprimir → el membrete se repite y el texto respeta la banda en cada hoja. Pulsar Descargar PDF → hoja 1 con membrete correcto; hoja 2 sin membrete y con el texto empalmado arriba (donde iría el encabezado) y abajo (donde iría el pie).
- Arreglo: Unificar la salida PDF de la nota membretada con el mismo motor de Imprimir en vez de html2canvas: (a) opción robusta = generar el PDF desde el popup de print-element (misma envoltura tabla thead/tfoot + membrete fixed) y usar 'imprimir a PDF' del navegador, o (b) construir el PDF por páginas con jsPDF reservando las bandas mMemb.top/bottom y dibujando el membrete como fondo en CADA página. Mínimo viable: en descargarPDF, cuando membrete!=null, clonar #doc dentro de la misma estructura de tabla espaciadora y pintar .membrete-bg como fondo repetido (position:fixed) para que el paginado de html2pdf reserve la banda en todas las hojas. Que Imprimir y PDF compartan una sola función de render membretado.

### 11. [P1] src/app/(dashboard)/receta/[patientId]/[notaId]/page.tsx:331 — Descargar PDF de RECETA y ORDEN vs Imprimir (estado: diseño APAISADO cuya hoja NO se reduce a host-carta, p.ej. diseño mayor que carta)
**Receta/Orden: el PDF fuerza orientation:'portrait' mientras Imprimir toma la orientación real de la hoja (cssPage)**
- Impacto: En el PDF se pasa siempre orientation:'portrait' con format:[host.widthMm, host.heightMm] (receta page.tsx:329-334; orden page.tsx:460-464). Cuando la hoja NO cae en HostCarta (dimensionesImpresion devuelve esHostCarta=false y widthMm>heightMm, caso de un diseño apaisado que no cabe reducido en carta), jsPDF normaliza el formato al pedir 'portrait' e intercambia ancho/alto → página vertical para un arte horizontal → el diseño sale reducido con letterbox o recortado. Imprimir NO tiene ese problema: usa @page size:${cssPage} = '${w}mm ${h}mm' (page.tsx:769) que respeta la orientación real de la hoja. Resultado: Imprimir horizontal, PDF vertical para el mismo formato. Nota: para media-carta apaisada sí se reduce a HostCarta carta-vertical y ambas coinciden; el desajuste aparece en diseños apaisados iguales o mayores que carta.
- Repro: Subir un diseño de receta APAISADO cuyo tamaño configurado no sea menor que carta (o forzar imprimirEn distinto de la reducción a carta). Imprimir → hoja horizontal, el arte llena la hoja. Descargar PDF → página vertical, el arte apaisado queda encajado/recortado.
- Arreglo: Derivar la orientación del PDF de la propia hoja en vez de fijar 'portrait': orientation = host.widthMm > host.heightMm ? 'landscape' : 'portrait' (usar el mismo dato que produce cssPage). Idealmente exponer desde dimensionesImpresion la orientación junto con cssPage y consumirla tanto en @page como en descargarComoPDF para que Imprimir y PDF usen exactamente la misma fuente de verdad.

### 12. [P1] src/app/(dashboard)/receta/[patientId]/[notaId]/page.tsx:332 — DESCARGAR PDF de receta y de orden (misma falla en orden/[...]/page.tsx:463), con diseño propio APAISADO más ancho que carta
**El PDF de receta/orden fuerza orientation:'portrait' e ignora el diseño APAISADO → sale volteado/recortado, mientras Imprimir y la vista previa salen apaisados**
- Impacto: En 'Descargar PDF' se pasa siempre orientation:'portrait' con format:[host.widthMm, host.heightMm]. Cuando el diseño subido es apaisado y NO cabe en carta (membrete apaisado tamaño carta 279×216, A4 apaisado 297×210, oficio o carta horizontal), dimensionesImpresion() devuelve host landscape (w>h) y jsPDF, al recibir orientation 'portrait', INTERCAMBIA las dimensiones a portrait (p.ej. [279,216]→página 216×279): el contenido apaisado se comprime/recorta en una página vertical. En cambio Imprimir usa @page size:'279mm 216mm' (paperEfectivo.cssPage, landscape) y la vista previa usa host landscape → salen bien. Resultado: el MISMO documento sale correcto en pantalla e Imprimir pero deformado/mocho en el PDF. Nota: el caso media-carta/A5 apaisado NO se ve porque cae a HostCarta (carta vertical) y ahí portrait sí es correcto; la falla es específica de diseños apaisados > 216 mm de ancho.
- Repro: Config → Recetas: subir un membrete APAISADO tamaño carta o A4 (ancho > 216 mm). Generar una receta. Imprimir = hoja horizontal correcta; Descargar PDF = página vertical con el diseño achatado/recortado. Comparar ambos.
- Arreglo: Derivar la orientación del host en ambas páginas: `orientation: host.widthMm > host.heightMm ? 'landscape' : 'portrait'` (host = dimensionesImpresion(recetaConfigOri)), en receta/[...]:329-334 y orden/[...]:460-465. Así PDF coincide con @page/preview en las 4 salidas.

### 13. [P1] src/app/(dashboard)/configuracion/secciones-recetas.tsx:239 — PANTALLA (preview de la página y del calibrador), IMPRIMIR y DESCARGAR PDF de receta y orden, cuando el diseño se subió como imagen (no PDF)
**Subir el diseño como IMAGEN (PNG/JPG, foto/escaneo) guarda milímetros absurdos (asume 96 DPI) → el tamaño de hoja diverge: RecetaDocumento renderiza según paperSize (140 mm) pero preview/@page/PDF de la página usan ~688 mm**
- Impacto: Para imágenes se calcula widthMm=(px*25.4)/96 y heightMm igual (líneas 239-240) y se GUARDAN en disenoWidthMm/HeightMm (línea 271). Un escaneo/foto real (200-300 DPI, o el tope de 2600 px) da ~437-688 mm en vez de 140 mm; detectarPaperSize() devuelve null (fuera de tolerancia ±5) y paperSize no se corrige. Hay DOS fuentes de verdad para el tamaño de hoja: RecetaDocumento (RecetaDocumento.tsx:241-245) IGNORA disenoWidthMm y recalcula la hoja desde PAPER_SIZES[paperSize] (≈140 mm), pero la página consume paperEfectivo() (RecetaDocumento.tsx:118) que SÍ usa disenoWidthMm (~688 mm) para RecetaPreviewWrapper, el @page de impresión y el format del PDF. Consecuencia: la hoja real mide 140 mm pero el marco de preview, el @page y el PDF se dimensionan a ~688 mm → preview con la hoja diminuta en un marco gigante, e Imprimir/PDF con una página de ~688 mm casi vacía. Síntoma visible: el rótulo del calibrador imprime «tu formato (688×888 mm)» (secciones-recetas.tsx:824-825).
- Repro: Config → Recetas → subir el recetario como foto/JPG o PNG escaneado (no como PDF). Ver el rótulo «tu formato (NNN×NNN mm)» con cientos de mm. Generar receta: la vista previa muestra la hoja minúscula descentrada y Imprimir/PDF salen a un tamaño de página enorme.
- Arreglo: Hacer congruente la fuente de verdad del tamaño. Opción mínima y segura: en la subida de imagen NO guardar disenoWidthMm/HeightMm si no corresponden a un tamaño físico real (omitirlos, o derivarlos de detectarPaperSize por relación de aspecto), de modo que paperEfectivo() caiga a PAPER_SIZES[paperSize] igual que RecetaDocumento. Alternativa: que RecetaDocumento honre disenoWidthMm/HeightMm cuando existan, para una sola fuente en las 4 salidas.

### 14. [P1] src/lib/receta-word.ts:149 — WORD (.doc) de receta y orden, para cualquier paperSize distinto de carta (media-carta, A5, A4, oficio) o diseño APAISADO
**El Word de receta/orden fija SIEMPRE hoja carta vertical (21.6×27.9 cm) sin importar el papel configurado ni la orientación del diseño**
- Impacto: El @page del Word está hardcodeado a `size: 21.6cm 27.9cm` (carta vertical) con márgenes 2/2.2 cm, ignorando recetaConfig.paperSize y la orientación. Una receta configurada en A5/media-carta (el caso real del Dr, según memoria su config quedó en A5) o un diseño apaisado sale en Word a carta vertical: tamaño y orientación distintos a los que producen Imprimir, PDF y pantalla, que sí respetan paperEfectivo. Además el encabezado usa recetaConfig.membreteDataUrl (membrete de la plantilla generada), no disenoCompletoDataUrl (el diseño completo subido) → el Word ni siquiera lleva el diseño que sí usan las otras 3 salidas. Es incongruencia de tamaño/orientación entre salidas del mismo documento.
- Repro: Config → Recetas con paperSize A5 (o media-carta). Generar receta → botón Word. El .doc abre en carta vertical con márgenes anchos, no en A5, a diferencia del PDF/Imprimir/preview que salen en A5.
- Arreglo: Derivar el @page del Word de paperEfectivo(recetaConfig): usar `${p.widthMm}cm/mm ${p.heightMm}...` y ajustar orientación cuando el diseño sea apaisado (widthMm>heightMm), para que el tamaño/orientación del .doc coincida con las otras salidas; y, si hay disenoCompletoDataUrl, embeberlo como fondo de página en vez del membreteDataUrl.

### 15. [P1] src/app/(dashboard)/orden/[patientId]/[notaId]/page.tsx:397 — Orden: PANTALLA, IMPRIMIR y PDF (diseño/membrete de receta-orden). Estado: consultorio con VARIOS médicos, diseño por-médico subido.
**Orden: el diseño por-médico (recetasPorMedico) se resuelve con un `unicoMedico` congelado — puede estampar el formato de OTRO médico**
- Impacto: El useMemo de `recetaConfig` llama a `entradaPorMedico(config?.recetasPorMedico, medicoId, overrideRecetaValido, unicoMedico)` (línea 392) pero su lista de dependencias (línea 397) NO incluye `unicoMedico`. Es EXACTAMENTE el bug que la página de receta ya corrigió y documentó en su línea 217 ('unicoMedico llega tarde desde useDoctors… la plantilla podía quedarse con el valor inicial y desalinearse de la firma'). `unicoMedico` arranca en `true` (activeDoctors=[] ⇒ length 0). Si `config` llega antes que la subcolección `doctors` (habitual: config es 1 doc, doctors es una query), el memo calcula con unicoMedico=true en un consultorio multi-médico y toma el fallback de 'única entrada válida' de entradaPorMedico ⇒ imprime el DISEÑO/formato de otro médico en la orden. Cuando doctors carga (unicoMedico=false) el memo NO recomputa. Peor aún: `configFirma` (línea 416) SÍ tiene `unicoMedico` en deps y sí recomputa, así que el diseño (de otro médico) y la firma (suprimida) quedan incongruentes entre sí y respecto a la receta del mismo acto.
- Repro: Consultorio con 2+ médicos; el médico A subió su diseño de receta/orden, B no. B firma una nota y abre Orden. Con la carrera config-antes-que-doctors, la orden se renderiza (pantalla/imprimir/PDF) sobre el ARTE de A. En la página de Receta del mismo paciente no ocurre porque su memo sí depende de unicoMedico.
- Arreglo: Igualar la página de orden a la de receta: `}, [config, nota?.metadata?.medicoId, unicoMedico, overrideRecetaValido])` en la línea 397. Idealmente extraer la resolución de plantilla+firma a un hook compartido para que receta y orden no puedan divergir.

### 16. [P1] src/components/RecetaDocumento.tsx:716 — Receta y Orden en las CUATRO salidas: PANTALLA, IMPRIMIR, PDF (RecetaDocumento.tsx:716-718) y WORD (receta-word.ts:51-53). Estado: consultorio con varios médicos; la nota la firmó un médico distinto al de `config.nombreMedico`.
**Receta/Orden imprimen el NOMBRE y CÉDULA del médico por defecto de la clínica, no el del autor de la nota — mientras la firma sí se resuelve por-médico**
- Impacto: `HojaGenerada` toma `medico = config?.nombreMedico`, `cedula = config?.cedulaProfesional`, `especialidad = config?.especialidad` (líneas 716-718), y receta-word hace lo mismo (líneas 51-53). Las páginas pasan `configFirma`, que SOLO sobrescribe `firmaImagenDataUrl` (resuelto por-médico vía entradaPorMedico), no el nombre/cédula. Resultado: la receta/orden atribuye la prescripción a 'Dr. A · Cédula A' (el dueño/predeterminado) aunque la nota la haya escrito Dr. B, y puede llevar la firma gráfica de B encima ⇒ documento legalmente contradictorio (nombre de A, firma de B). La NOTA ya lo resuelve bien usando el snapshot `nota.firma` (nota page líneas 164-166; nota-word líneas 28-30); receta/orden ni siquiera reciben `nota.firma`.
- Repro: Clínica con dueño A (config.nombreMedico=A) y médico B. B firma una nota (nota.firma.nombreMedico=B, cédula B). Generar Receta u Orden desde esa nota: el encabezado auto y el pie de firma dicen 'A / Cédula A' en pantalla, impresión, PDF y Word, aunque el acto lo prescribió B.
- Arreglo: Pasar `nota.firma` a RecetaDocumento y a descargarRecetaWord y resolver `medico/cedula/especialidad` con la misma precedencia que la nota: `nota.firma?.nombreMedico || config?.nombreMedico`, etc. Así las 4 salidas de receta/orden atribuyen al autor real, congruente con la NOTA y con la firma gráfica.

### 17. [P1] src/app/(dashboard)/receta/[patientId]/[notaId]/page.tsx:269 — Receta y Orden (PANTALLA/IMPRIMIR/PDF) frente a la NOTA del mismo acto. receta page:269, orden page:411. Estado: consultorio multi-médico con nota ya firmada.
**Receta/Orden ignoran el snapshot inmutable `nota.firma.imagenDataUrl` y re-resuelven por el camino uid↔doctors que casi siempre falla**
- Impacto: La NOTA muestra la firma con precedencia `nota.firma?.imagenDataUrl || entradaPorMedico(config.firmaPorMedico, medicoId, …) || (unicoMedico ? global)` (nota page línea 186-188). Receta/Orden, en cambio, calculan `configFirma` SIN consultar el snapshot de la nota: `entradaPorMedico(config.firmaPorMedico, medicoId, firmaValida, unicoMedico) || (unicoMedico ? config.firmaImagenDataUrl : undefined)` (receta 269-272, orden 411-414). Como el propio impreso-medico.ts documenta, el match por `medicoId=uid` contra las llaves de `doctors` casi siempre falla; en multi-médico entradaPorMedico devuelve undefined y NO hay fallback ⇒ la receta/orden salen SIN firma y disparan 'sinFirmaResoluble', aunque la nota firmada YA contiene el snapshot de la firma de ese médico y la NOTA sí la imprime. Dos documentos del mismo acto: la nota firmada, la receta sin firmar.
- Repro: Clínica con 2+ médicos. B firma una nota (queda nota.firma.imagenDataUrl con su firma+sello). La NOTA impresa muestra la firma de B. Abrir Receta u Orden desde esa misma nota: banner 'saldrá sin firma ni sello' y documento sin firma, pese a existir el snapshot.
- Arreglo: Anteponer el snapshot de la nota en el cálculo de `configFirma` de receta y orden, igual que la nota: `const firma = nota?.firma?.imagenDataUrl || entradaPorMedico(config.firmaPorMedico, medicoId, firmaValida, unicoMedico) || (unicoMedico ? config.firmaImagenDataUrl : undefined)`. Congruente con la NOTA y sin riesgo de suplantación (es la firma que YA quedó sellada en esa nota).

### 18. [P1] src/app/(dashboard)/nota/[patientId]/[notaId]/page.tsx:275 — Nota — Pantalla (preview) y Descargar PDF (incongruentes contra Imprimir)
**Nota multipágina: el membrete de fondo se pinta UNA sola vez en Pantalla y en PDF; su pie cae a media hoja y las páginas 2+ salen sin membrete**
- Impacto: El <img className="membrete-bg"> se posiciona con top:0 y height:auto, es decir se dibuja UNA vez, anclado al inicio del #doc. El pie del arte membretado (domicilio/franja inferior) cae en el punto donde termina la altura natural de la imagen = a media hoja 1, encimándose con el texto de la nota; y en las hojas 2+ NO hay membrete. En Imprimir esto SÍ está resuelto (print-element.ts modo 'membrete' usa membrete-bg position:fixed inset:0 + tabla thead/tfoot que repite la banda en CADA hoja), pero descargarPDF() rasteriza #doc como UN lienzo alto con html2canvas y luego lo rebana en páginas carta, arrastrando el membrete único. Resultado: tres salidas del MISMO documento se ven distinto (Imprimir bien, Pantalla y PDF mal). Es exactamente la incongruencia que el dueño exige eliminar.
- Repro: Config con hoja membretada de nota. Abrir una nota larga (2+ páginas: varias secciones/plan). Pantalla: el pie del membrete aparece a mitad de la hoja sobre el texto. Descargar PDF: página 1 con membrete parcial y texto encimado, página 2 SIN membrete y con el texto pegado al borde superior. Imprimir (Ctrl+P → popup): el membrete se repite correcto en cada hoja. Comparar las tres.
- Arreglo: Unificar el modelo de las 3 salidas al que YA funciona en Imprimir: para Pantalla/PDF paginar el #doc en hojas carta reales y pintar el membrete como fondo por-hoja (position:fixed/repetido) con banda superior/inferior reservada (mismo mMemb que print-element), en vez de un único <img top:0 height:auto>. Alternativa mínima para el PDF: generar el PDF desde el MISMO HTML que arma print-element (tabla espaciadora + membrete fixed) y no desde #doc plano. Así Pantalla=Imprimir=PDF.

### 19. [P1] src/lib/nota-word.ts:37 — Nota — Word (.doc) (incongruente contra Pantalla/Imprimir/PDF que sí muestran el membrete)
**Word de la NOTA nunca usa la hoja membretada: siempre imprime el encabezado de texto, aunque el médico tenga membrete configurado**
- Impacto: construirNotaHTML() arma SIEMPRE el bloque `encabezado` con nombre/cédula/establecimiento en texto y nunca lee config.notaMembreteDataUrl ni notaMembretePorMedico. El mismo acto médico sale con la hoja membretada en las otras 3 salidas y sin ninguna marca de la clínica en Word. Contradice la ley del dueño ('si hay membrete, úsalo; congruente incluso en Word').
- Repro: Con hoja membretada de nota configurada, abrir una nota → botón Word. El .doc descargado trae el encabezado de texto genérico, sin el membrete que sí aparece en Pantalla/Imprimir/PDF.
- Arreglo: En construirNotaHTML, resolver el membrete igual que la página (entradaPorMedico(config.notaMembretePorMedico…) → notaMembreteDataUrl) y, si existe, sustituir el bloque de texto por <img src="URL_ABSOLUTA" style="width:100%"> con márgenes mso equivalentes a notaMembreteMargenes; caer al encabezado de texto solo cuando NO haya membrete (mismo default que las otras salidas). Absolutizar la ruta /api/receta/... como ya hace receta-word.ts.

### 20. [P1] src/lib/receta-word.ts:69 — Receta y Orden — Word (.doc) (incongruente contra Pantalla/Imprimir/PDF que renderizan el diseño completo)
**Word de receta/orden usa solo membreteDataUrl e IGNORA el diseño calibrado (disenoCompletoDataUrl): con el diseño principal, el Word sale como texto sin branding**
- Impacto: El bloque `membreteSrc` solo lee recetaConfig.membreteDataUrl (el membrete-imagen simple del template auto-generado). El camino PRINCIPAL del dueño es 'diseño de receta subido' = disenoCompletoDataUrl + disenoCampos (hoja completa calibrada), que RecetaDocumento usa en Pantalla/Imprimir/PDF. receta-word.ts nunca lo mira, así que cuando el médico tiene su diseño propio (y membreteDataUrl vacío) el Word cae al encabezado de texto azul genérico: cero membrete, cero arte, en receta y orden por igual (orden usa el mismo descargarRecetaWord). Es un medio-arreglo que da falsa confianza: 'el Word sí soporta membrete' pero solo el que casi nadie usa.
- Repro: Subir un DISEÑO de receta (imagen completa) en Configuración → Recetas (disenoCompletoDataUrl). Generar receta → Word. El .doc sale con encabezado de texto, sin el diseño que se ve en pantalla y sale al imprimir/PDF. Igual en Orden.
- Arreglo: En construirRecetaHTML priorizar el diseño: si recetaConfig.disenoCompletoDataUrl existe, usar ESA imagen a sangre como fondo/encabezado (URL absoluta) y colocar los datos del paciente conforme a disenoCampos, en vez de membreteDataUrl; membreteDataUrl como segundo caso y el texto como default. Congruente con RecetaDocumento y con Word de nota (ver hallazgo previo).

### 21. [P2] src/app/(dashboard)/nota/[patientId]/[notaId]/page.tsx:68 — Imprimir vs. PDF de la nota SIN membrete (y de la carta de referencia)
**NOTA sin membrete: Imprimir usa márgenes 18mm y PDF 12mm — misma nota, distinta paginación entre salidas**
- Impacto: El PDF llama descargarComoPDF con format 'letter' y margen default 12mm (pdf-download.ts línea 29); Imprimir abre el popup con formato:'carta' que fija @page margin:18mm (print-element.ts línea 111). El <style> @page margin:1.5cm de la propia página (línea 570) NO llega al popup porque imprimirElemento solo copia los <link>, no los <style>. Resultado: la misma nota refluye distinto y los cortes de página caen en lugares diferentes entre Imprimir y PDF. La referencia tiene el mismo desfase (PDF default 12mm vs Imprimir 18mm).
- Repro: Nota larga SIN membrete: Imprimir → Guardar como PDF (márgenes ~18mm) y comparar con 'Descargar PDF' (márgenes 12mm): distinto ancho de texto y distinto salto de página.
- Arreglo: Unificar el margen del formato 'carta' entre las dos rutas: pasar `margin: 18` (o el valor de @page) a descargarComoPDF para nota/referencia sin membrete, o parametrizar ambos desde una sola constante de márgenes de documento de texto.

### 22. [P2] src/lib/receta-word.ts:149 — WORD de receta y orden (incongruente con IMPRIMIR/PDF que respetan paperSize)
**El Word de receta fija tamaño CARTA por hardcode e ignora recetaConfig.paperSize (A5/media-carta/oficio)**
- Impacto: El @page está codificado a 21.6cm x 27.9cm (carta) sin mirar recetaConfig.paperSize. Imprimir y PDF usan dimensionesImpresion(recetaConfigOri) y respetan A5/media-carta/oficio (y la orientación del membrete apaisado, vía useRecetaPaperOrientado). El Dr, cuya config quedó en A5 (según memoria del proyecto), obtiene un Word en hoja carta: tamaño y proporciones distintas a su receta impresa.
- Repro: recetaConfig.paperSize='a5'. Imprimir/PDF salen A5; Descargar Word => página carta.
- Arreglo: Mapear recetaConfig.paperSize (y, si hay diseño, disenoWidthMm/HeightMm ya orientados) a la medida real y emitir @page con esas dimensiones, igual que dimensionesImpresion. Para media-carta/A5 usar la medida exacta del papel.

### 23. [P2] src/lib/receta-word.ts:156 — WORD de receta (incongruente con IMPRIMIR/PDF que llevan QR)
**El Word de receta no lleva QR de verificación: solo folio en texto, mientras Imprimir/PDF estampan el QR firmado**
- Impacto: mostrarQR viene true por defecto (receta page:200) y la receta impresa/PDF codifica un QR firmado HMAC hacia /verificar/<token> ligado a la huella del contenido. El Word solo imprime 'Folio: X' en texto (línea 156) y no incluye QR ni el enlace de verificación. Una receta nacida del Word no es verificable por la farmacia/paciente, a diferencia de las otras salidas.
- Repro: Con mostrarQR activo, comparar la receta impresa (con QR) contra el .doc de Word (sin QR).
- Arreglo: Generar en cliente el mismo QR (data-URI PNG del token de verificación) y embeberlo en el HTML del Word junto a la firma/folio, condicionado a recetaConfig.mostrarQR; si hay disenoCampos.qr, colocarlo en esa posición al integrar el diseño (hallazgo 1).

### 24. [P2] src/lib/pdf-download.ts:29 — Descargar PDF vs Imprimir en NOTA sin membrete y CARTA DE REFERENCIA (documentos de texto en formato 'carta')
**Modo 'carta': Imprimir usa márgenes de 18mm y el PDF los 12mm por defecto → paginación y blancos distintos**
- Impacto: print-element en formato 'carta' aplica @page{margin:18mm} (print-element.ts:111). El PDF de esos mismos documentos llama descargarComoPDF sin margin, tomando el default de 12mm (pdf-download.ts:29): referencia page.tsx:53 y nota sin membrete page.tsx:68 (solo pasa margin:0 cuando hay membrete). Un margen de 18mm vs 12mm cambia el ancho útil → el texto se ajusta y pagina distinto entre Imprimir y PDF, y el PDF llega visiblemente más cerca del borde. Rompe la congruencia entre salidas incluso sin membrete (donde ambas deberían ser idénticas).
- Repro: Abrir una carta de referencia (o una nota sin hoja membretada) con texto que llene la hoja. Imprimir → márgenes ~18mm. Descargar PDF → márgenes ~12mm; el corte de página y los renglones no coinciden con la impresión.
- Arreglo: Centralizar el margen de 'carta' en una constante compartida y pasársela a descargarComoPDF en los documentos de texto (p.ej. margin:18 en referencia y en la nota sin membrete), de modo que print-element y pdf-download usen el mismo valor. Alternativamente unificar ambos por el mismo motor de impresión.

### 25. [P2] src/lib/print-element.ts:105 — Nota — Imprimir vs Pantalla/PDF (tamaño y posición del arte del membrete)
**El membrete de la nota se escala/posiciona distinto entre Imprimir (object-fit:contain a página carta completa) y Pantalla/PDF (height:auto a proporción natural)**
- Impacto: En Imprimir el membrete-bg va position:fixed; inset:0; height:100%; object-fit:contain → se ajusta (con letterbox) a la hoja carta COMPLETA. En Pantalla/PDF el mismo <img> va width:100%; height:auto → se dibuja a su proporción natural desde arriba. Si el arte del médico NO tiene proporción carta (216×279) —p. ej. membrete apaisado, A4, o solo franja superior— el logo aparece a un tamaño y a una altura en Imprimir y a OTRO tamaño/altura en Pantalla/PDF. Es el mismo tipo de bug de orientación ya cazado en receta, ahora en la hoja de nota. Suma al hallazgo del membrete único.
- Repro: Configurar como hoja membretada de nota una imagen que no sea carta (p. ej. 1000×400 apaisada o A4). Comparar Imprimir (arte centrado y ajustado a la hoja) contra Pantalla/PDF (arte estirado a lo ancho desde el borde superior, distinta escala). El logo cambia de tamaño entre salidas.
- Arreglo: Fijar UN criterio de encaje del membrete para las 3 salidas (idealmente hoja carta con object-fit:contain, como Imprimir) y aplicarlo también al render de Pantalla/PDF; documentar/validar la proporción del arte al subirlo (avisar si no es carta) para que el pie caiga siempre en el mismo lugar.

### 26. [P2] src/components/RecetaDocumento.tsx:731 — Nota vs Receta/Orden — todas las salidas cuando NO hay diseño subido
**El default 'sin diseño' no es tipográficamente coherente entre documentos: Nota en serif (Times) vs Receta/Orden en sans (Inter/Calibri)**
- Impacto: El dueño pide que sin diseño 'todas usen el mismo default limpio'. Hoy la Nota por defecto usa 'Times New Roman' serif con filete 2px (page.tsx nota linea 257 y nota-word.ts linea 76), mientras la Receta/Orden default usa Inter/Helvetica/Georgia sans con acentos de color (RecetaDocumento fontFamily linea 727-731) y el Word de receta usa Calibri (receta-word.ts linea 150). Documentos del mismo consultorio, mismo médico, salen con identidad tipográfica distinta según sean nota o receta, y aun dentro de la receta la fuente de pantalla (Inter) difiere de la del Word (Calibri). Rompe la percepción de 'papelería congruente'.
- Repro: Sin membrete ni diseño subido, generar una Nota y una Receta del mismo paciente e imprimir ambas: la nota sale serif con doble filete, la receta sans con franja de color. El Word de receta usa Calibri, no la Inter de pantalla.
- Arreglo: Definir un sistema tipográfico único para el default limpio (una familia base para encabezado/cuerpo y un acento) y aplicarlo por igual a nota, receta y orden en las 4 salidas (pantalla, print-element, PDF y las plantillas Word), o al menos alinear serif/sans y el tratamiento de encabezado entre documentos.

### 27. [P3] src/app/(dashboard)/nota/[patientId]/[notaId]/page.tsx:568 — Impresión nativa del navegador (no el botón Imprimir) de NOTA membretada y REFERENCIA
**Impresión nativa (Cmd/Ctrl+P) de nota/referencia: el @media print residual ignora el membrete y pisa los márgenes del botón Imprimir**
- Impacto: Cada pantalla conserva un bloque @media print heredado (nota page.tsx:564-572; referencia page.tsx:251-259) que hace #doc{padding:24px 28px} y @page{margin:1.5cm}. Ese camino NO envuelve el contenido en la tabla espaciadora ni convierte el membrete a fixed, y además sobrescribe el padding mMemb del #doc. Si el usuario imprime con Cmd/Ctrl+P en vez del botón, la nota membretada sale con el membrete una sola vez (img absolute top:0), sin repetir por página y con los márgenes cambiados: resultado distinto e inconsistente frente al botón Imprimir (que sí usa print-element correctamente). El botón funciona porque print-element no copia los <style> en línea; el atajo nativo no tiene esa protección.
- Repro: En una nota membretada de 2+ páginas, en vez del botón Imprimir usar el menú del navegador (Cmd/Ctrl+P). Salida sin membrete repetido y con márgenes 1.5cm/padding 24px, distinta del botón Imprimir.
- Arreglo: Retirar (o neutralizar para el caso membrete) el @media print residual de nota y referencia, y guiar toda la impresión por el botón Imprimir/print-element; o, si se quiere soportar el atajo nativo, replicar en ese @media print la lógica del membrete (fondo fixed repetido + bandas) para que ambos caminos den el mismo documento.
