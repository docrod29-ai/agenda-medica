# Decisiones aplicadas por omisión — CONSULTA (Panel de Lujo 2026-09)

Cuando un hallazgo dependía de una decisión del dueño y no había recomendación
por omisión en `04-DECISIONES-DEL-DUENO.md`, se aplicó el **valor seguro** del
briefing: escalar en vez de contestar, preguntar en vez de asumir, **mostrar en
vez de esconder**, y no inventar ninguna cifra clínica.

| Hallazgo | Decisión aplicada | Dónde cambiarla | Por qué es la segura |
|---|---|---|---|
| ASN-002 | Compuerta de plausibilidad que **avisa y no bloquea**, con las bandas que YA existían en el repositorio: `RANGOS_UCI` (`src/lib/uci/extraccion.ts`, REG-031/REG-036) para TA, FR, SpO₂ y temperatura; `pesoAKg`/`tallaACm` (`src/lib/expediente/parser-clinico.ts`) para peso y talla. No se inventó ninguna. | `RANGOS_PLAUSIBLES` en `src/app/(dashboard)/consulta/[patientId]/signos-que-se-capturan.ts` | Preguntar no puede hacer daño; bloquear con una banda que nadie validó, sí. El valor tecleado se conserva tal cual. |
| ASN-002 (FC) | La **frecuencia cardiaca NO se vigila**: no hay banda con procedencia en el repositorio y el rango por edad es `NEEDS_CLINICAL_REVIEW` (la pregunta al médico ya está abierta en `registry.ts:1293`). Se declara en pantalla y en el módulo. | `SIN_RANGO_DECLARADO` y `POR_QUE_LA_FC_NO_SE_VIGILA`, mismo archivo | clinical-safety §5: que falte un rango significa que ese caso no se mira, no que se dé por bueno. Inventar «40–200» habría sido una cifra clínica sin fuente. |
| ASN-005 | La talla ≤ 3 pregunta «¿querías decir metros?» usando el criterio que ya aplica `tallaACm` («sin unidad y ≤ 3 son metros»). No convierte: pregunta. | `tallaPareceEnMetros`, mismo archivo | Convertir en silencio es la regla 3 al revés. |
| ASN-012 | C-5 sigue pendiente (¿motivo obligatorio? ¿aplica a Practice?). Se aplica el valor seguro: **el motivo se pide y NO bloquea**; sin motivo la constancia dice «sin motivo declarado», y la constancia se escribe **dentro de la nota** (sección «Correcciones de signos vitales»), que es donde sobrevive sin tocar tipos ni reglas de otra rebanada. | `src/app/(dashboard)/consulta/[patientId]/page.tsx`, bloque ASN-012 | Mostrar en vez de esconder; y no bloquear a un médico por una decisión que el dueño no ha tomado. |
| D-001 | Los dos paneles de corrección **se muestran** en la consulta. GP4/GP12 no consta en ningún acta (`DECISION_LOG.md`, `V10_DECISION_LOG.md`): no era decisión del dueño. Se conserva la calma con jerarquía: llegan plegados y sólo se abren solos cuando un cambio toca una cifra. | `src/components/CambiosCifrasPanel.tsx` y `CorreccionesPanel.tsx` | clinical-safety §3 no admite excepción por ruta. Si el dueño decide esconderlos, se registra como decisión y se invierte la prueba, no al revés. |
| MC-018 | La lista de la OMS **se conserva** y sólo se enseña donde hay quirófano (nota postoperatoria o internamiento activo). No se retira: Hospital está en pausa (D-030), no borrado. | `mostrarChecklist` en la consulta | Esconder no es borrar, y el sub-hallazgo accionable (el resumen que no nombraba lo pendiente) se reparó igualmente. |
| MG-017 | Se quita el tope superior de edad para el panel de gineco y se conserva la edad mínima (10 años) con sexo capturado. El tope superior era `NEEDS_CLINICAL_REVIEW`; **no se sustituye por otro número**: se elimina el que no tenía fuente. | `esGineco` en la consulta | El tamizaje cervical del propio motor llega a 65 y el climaterio no tiene tope: apagar el panel entero escondía funciones sin criterio. |
| MP-011 | La barra dice «N vacunas corresponden por edad · verifica cartilla» y deja de abrirse sola. La edición de la Cartilla Nacional sigue siendo `NEEDS_CLINICAL_REVIEW` (pregunta abierta en `registry.ts:1839`). | `vacunasQueCorresponden` en la consulta | Ausencia de dato no es dato de ausencia: sin registro de lo aplicado, «atrasada» afirma un hecho que nadie verificó. |
| PG-003 · PI-003 · PO-016 | El texto del consentimiento se corrige para decir la verdad completa (nube del consultorio, `HORAS_DE_VIDA` leído del módulo, y el motivo real: poder comprobar de dónde salió cada frase). La redacción final es `NEEDS_LEGAL_REVIEW`, pero **un texto veraz e imperfecto es mejor que uno preciso y falso**. | `src/app/(dashboard)/consulta/[patientId]/consentimiento-de-grabacion.ts` | Decir de más sobre lo que se hace con la voz del paciente es el lado seguro. |
| PP-009 | Cuando el paciente es menor, el texto se dirige al padre, madre o tutor. El campo `otorgadoPor` (nombre y parentesco) **no se añade** aquí: es del tipo `Patient`, de otra rebanada, y su redacción legal es `NEEDS_LEGAL_REVIEW`. | mismo módulo · handoff a SEGURIDAD | Corregir el sujeto del texto no necesita permiso; guardar un dato nuevo del paciente, sí. |
| PC-012 · PI-008 | Se escribe `VERSION_DEL_CONSENTIMIENTO` junto al texto, para que exista la mitad que hoy se puede tener. Guardarla con el consentimiento queda en el handoff. | mismo módulo | La frecuencia («una vez por paciente») es decisión del dueño y se respeta; lo que faltaba era CONSTANCIA. |
| RT-004 | Las citas fuera de rango **se marcan, no se borran**, con el criterio que ya fijó `verificar-la-cita.ts:293`. Y si PubMed no contestó, la sección lo declara en su primera línea en vez de titularse como si tuviera respaldo. | `src/app/(dashboard)/consulta/[patientId]/citas-del-analisis.ts` | Borrar le quitaría al médico un razonamiento que puede ser bueno; dejarlo con aspecto de respaldado es lo que no puede seguir pasando. |
| ZC-014 | Los umbrales sin fuente **no se retiran ni se cambian**: el texto que va a la nota dice «umbral orientativo, sin fuente citada» y lleva la unidad capturada. Citar organismo para cada uno es `NEEDS_CLINICAL_REVIEW` del dueño. | `src/components/PanelPreventivo.tsx` | Quitar el aviso perdería una señal útil; dejarlo sin decir que no tiene fuente lo hace pasar por criterio validado. |
| ZC-015 | El texto insertado lleva el organismo de cada prueba y la advertencia de vigencia. El contenido del catálogo sigue sin validar (marcado en el registro y ahora también con `SelloMotor`). | mismo archivo | Lo que entra al expediente tiene que poder defenderse solo. |
| MI-003 | De las dos salidas que ofrecía el hallazgo (pintar el sello, o corregir el texto de `/cumplimiento/motores`) se eligió **pintar el sello**: mostrar en vez de esconder. | los cuatro paneles con motores sin validar | La pantalla de cumplimiento decía la verdad sobre lo que el producto debería hacer; lo que faltaba era hacerlo. |
| MP-006 | El peso en duda hace que el motor de dosis por kilo reciba `undefined` **y la pantalla lo dice** («mientras tanto la revisión de dosis por kilo no corre»). No se calcula con un peso que no se puede defender. | `pesoParaDosis` en la consulta | Fallar cerrado y en voz alta. Un cálculo con el peso equivocado es peor que no calcular. |
| N-021 | **No se toca.** Publicar una demostración con audio actuado en la página pública es decisión del dueño y roza `data-privacy.md`. | — | El valor seguro aquí es no publicar. Ver `no-reparado-CONSULTA.md`. |
