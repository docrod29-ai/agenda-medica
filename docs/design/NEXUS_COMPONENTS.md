# Nexus Components

Normalización V14 sobre `COMPONENT_INVENTORY.md` (inventario real, generado).

Reglas V14 que gobiernan todo componente nuevo o tocado:

- obedece los tokens del Lock (nada de valores sueltos: el trinquete de diseño
  lo caza);
- radios 4/10/14, elevación plano/elevado únicamente;
- controles interactivos son `<button>`/elementos nativos (compuerta a11y);
- ningún componente-firma (Banda, Sello, Course Bar) se improvisa dos veces:
  se construye una vez y se reutiliza;
- primitivos compartidos antes que estilo en línea — la deuda medida (88,5 %
  de archivos con estilo en línea, auditoría V9) sólo puede bajar.
