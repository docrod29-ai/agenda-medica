# Nexus Screen Inventory

El inventario real y generado vive en `SCREEN_INVENTORY.md` (lo deriva
`scripts/` y tiene guardián — no se edita a mano; REG del guardián que «no
podía fallar» ya reparado).

V14 añade sobre ese inventario, por pantalla crítica: score visual, score de
identidad (Logo-Off), score generic-AI y evidencia (capturas desktop+móvil).
Esos scores viven en `agent-state/V14_VISUAL_SCORECARD.json` y nacen vacíos:
sin captura no hay número.
